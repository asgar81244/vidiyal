import 'package:flutter/material.dart';

class OurWorkPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Our Work'),
      ),
      body: Center(
        child: Text('Discover Our Work here.'),
      ),
    );
  }
}
